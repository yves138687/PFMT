# PFMT Agent Guidance

## Local Execution

- 简单文件路径查询优先使用 `pwsh`/PowerShell 7。
- 复杂搜索、复杂参数或多行命令优先使用 `rg`。
- 较大或较重的处理任务可以使用 Python 脚本执行。
- Python 项目运行前先确认所需 Python 版本和是否已有可复用环境。

## Hidden Content Rules

- PFMT 只有“展示/隐藏”一条可见性规则，不向用户暴露“私密目录”或“私密文件”作为业务选项。
- 文件和目录是否隐藏只以 `is_hidden` 为准；`path_type`、`visibility_type` 中的 `private` 只能作为历史兼容字段，不得作为新功能入口或用户侧概念。
- 隐藏文件、隐藏目录及其内容默认不得展示给用户，也不得进入 AI 检索、摘要、问答、提示词上下文或其它 AI 处理链路。
- 只有用户在当前会话中主动打开“显示隐藏文件/显示隐藏内容”后，才允许本次会话内展示隐藏内容。
- “显示隐藏文件/显示隐藏内容”是会话态开关，不持久化为默认展示策略；新会话默认关闭。
- 后端接口、前端列表、详情页、目录树和未来 AI 功能都必须按同一规则过滤隐藏内容。
